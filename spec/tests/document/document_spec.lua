local Document = require("hnetxt-nvim.document")
local List = require("hnetxt-nvim.document.element.list")

local doc, actual, expected

describe("*", function()
    before_each(function()
        doc = Document()
    end)

end)
